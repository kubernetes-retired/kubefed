apiVersion: core.federation.k8s.io/v1alpha1
kind: FederatedIngress
metadata:
  name: placeholder
  namespace: placeholder
spec:
  template:
    spec:
      backend:
        serviceName: testsvc
        servicePort: 80
