apiVersion: generated.federation.k8s.io/v1alpha1
kind: FederatedClusterRoleBinding
metadata:
  name: placeholder
spec:
  template:
    subjects:
    - kind: Group
      name: test-user
    roleRef:
      kind: ClusterRole
      name: cluster-admin
