apiVersion: federation.k8s.io/v1alpha1
kind: FederatedService
metadata:
  name: placeholder
  namespace: placeholder
spec:
  template:
    spec:
      type: ClusterIP
      ports:
        - name: http
          port: 80
